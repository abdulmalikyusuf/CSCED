function DonateNowBtn() {
  return (
    <button
      type="button"
      className="hidden lg:block px-4 md:px-8 py-1 md:py-3 bg-transparent text-primary border-2 border-primary hover:bg-primary hover:text-white transition-colors duration-300 delay-150 rounded-full text-xs md:text-sm uppercase font-medium"
    >
      donate now
    </button>
  );
}

export default DonateNowBtn;
